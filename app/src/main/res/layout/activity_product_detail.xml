<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Product Image -->
    <ImageView
        android:id="@+id/product_image"
        android:layout_width="match_parent"
        android:layout_height="520dp"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <ImageButton
        android:id="@+id/btnFavorite"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@android:color/transparent"
        android:scaleType="centerInside"
        android:layout_marginTop="40dp"
        android:layout_marginEnd="30dp"
        android:src="@drawable/ic_favorite"
        app:layout_constraintEnd_toEndOf="@id/product_image"
        app:layout_constraintTop_toTopOf="@id/product_image" />

    <!-- Sau ImageView, thêm View nền trắng -->
    <View
        android:id="@+id/white_background"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/bg_white_rounded"
        android:translationY="-40dp"
        app:layout_constraintTop_toBottomOf="@id/product_image"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Product Name -->
    <TextView
        android:id="@+id/product_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:text="Cappuccino"
        android:textSize="35sp"
        android:textColor="@color/black"
        android:textStyle="bold"
        android:layout_marginTop="-100dp"
        android:layout_marginStart="8dp"
        app:layout_constraintTop_toBottomOf="@id/product_image"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.07" />

    <!-- Rating Container -->
    <LinearLayout
        android:id="@+id/rating_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:paddingTop="3dp"
        android:paddingBottom="3dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="-5dp"
        android:background="@drawable/bg_category_btn_selected"
        android:gravity="center_vertical"
        app:layout_constraintTop_toTopOf="@id/product_name"
        app:layout_constraintBottom_toBottomOf="@id/product_name"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Rating Star Icon -->
        <ImageView
            android:id="@+id/star_icon"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginEnd="8dp"
            android:contentDescription="Star icon"
            android:src="@drawable/ic_eva" />

        <!-- Rating Value -->
        <TextView
            android:id="@+id/productRating"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="4.7"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:textSize="16sp" />
    </LinearLayout>

    <!-- Size Label -->
    <TextView
        android:id="@+id/size_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="28dp"
        android:padding="8dp"
        android:text="Coffee Size"
        android:textSize="26sp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/product_name"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Size Options -->
    <LinearLayout
        android:id="@+id/size_options"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp"
        app:layout_constraintTop_toBottomOf="@id/size_label"
        app:layout_constraintStart_toStartOf="parent">

        <Button
            android:id="@+id/small_size"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Small"
            android:layout_marginStart="24dp"
            android:backgroundTint="#846046"
            android:textColor="#FFFFFF" />

        <Button
            android:id="@+id/medium_size"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Medium"
            android:backgroundTint="#A9A9A9"
            android:layout_marginStart="24dp" />

        <Button
            android:id="@+id/large_size"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Large"
            android:backgroundTint="#A9A9A9"
            android:layout_marginStart="24dp" />
    </LinearLayout>

    <!-- About Label -->
    <TextView
        android:id="@+id/about_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="About"
        android:textSize="26sp"
        android:layout_marginStart="28dp"
        android:layout_marginEnd="28dp"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/size_options"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Product Description -->
    <TextView
        android:id="@+id/product_description"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:textSize="16sp"
        android:padding="8dp"
        android:text="About ipsum dolor sit amet, consectetur adipiscing elit. Id ipsum varius vel lorem amet. Nunc id diam cras vulputate proin. Neque, habitasse id vulputate cras cras. Necue, vel duis vel elit pellentesue."
        app:layout_constraintTop_toBottomOf="@id/about_label"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Add to Cart Button -->
    <Button
        android:id="@+id/btn_add_to_cart"
        android:layout_width="300dp"
        android:layout_height="65dp"
        android:text="Add to Cart | 4$"
        android:textColor="#FFFFFF"
        android:textSize="22sp"
        android:backgroundTint="@color/black"
        android:layout_marginBottom="35dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>